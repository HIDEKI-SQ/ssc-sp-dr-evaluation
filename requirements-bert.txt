# Optional: only needed to REGENERATE the BERT embeddings from concept labels.
# Base reproduction does not require these; the precomputed embeddings are
# shipped under data/intermediate/bert_embeddings.npz.
# Recorded generation run used transformers 5.12.1 and torch 2.11.0+cpu.
transformers>=4.30.0
torch>=2.0.0
